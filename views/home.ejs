<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <form class="d-flex ms-auto">
            <a class="btn btn-outline-danger" href="/logout" role="button">Logout</a>
        </form>
    </nav>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-4 text-center">
                <img src="images/profile/<%= user.image%>" alt="User Image" class="rounded-circle img-fluid">
                <h3 class="mt-3">Welcome, <span id="usernameDisplay"><%=user.name%></span>!</h3>
            </div>
            <div class="col-md-8">
                <h4 class="mb-4">User Profile</h4>
                <form>
                    <div class="mb-3">
                        <label for="username" class="form-label">Gmail</label>
                        <input type="text" class="form-control" id="username" value="<%=user.email %>" disabled>
                    </div>
                    <div class="mb-3">
                        <label for="mobile" class="form-label">Mobile Number</label>
                        <input type="tel" class="form-control" id="mobile" value="<%=user.phone%>" disabled>
                    </div>
                    <div class="text-center">
                        <a type="button" class="btn btn-primary" id="editBtn" href="/home/edit">Edit</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
